{
    "cooldown_minutes": {
        "description": "冷却时间(分钟)",
        "type": "int",
        "hint": "用户两次使用答案之书的最小间隔时间",
        "default": 10,
        "minimum": 1,
        "maximum": 1440
    },
    "banned_keywords": {
        "description": "违禁关键词列表",
        "type": "text",
        "hint": "每行一个关键词，问题中包含这些词将被拒绝回答",
        "default": "政治\n色情\n反动\n恐怖\n暴力\n毒品\n赌博",
        "multiline": true,
        "placeholder": "每行输入一个关键词"
    }
}
